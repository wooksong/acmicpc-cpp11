#include <iostream>

int main() {
    int len_horizontal, len_vertical;

    // freopen("input.txt", "r", stdin);

    std::cin >> len_horizontal >> len_vertical;
    std::cout << len_horizontal * len_vertical << "\n";

    return 0;
}
